# Imagem do servidor Bjorn-cortex.
# Exemplo: ghcr.io/<usuario>/bjorn-cortex:latest
CORTEX_IMAGE=ghcr.io/infinition/bjorn-cortex:latest

# Porta HTTP exposta no host (host:container -> 8001:8000 por padrão)
CORTEX_PORT=8001

# Fuso horário do contêiner
TZ=America/Sao_Paulo
